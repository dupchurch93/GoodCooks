doctype html
html
  head
      block head
        title GoodCooks - #{title}
        link(rel='stylesheet' href='/stylesheets/reset.css')
  body
    if user
      div= user.id
      div= user.username
      div= user.email
    block content
